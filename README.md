# ERGUN. – Website

Statische Seite (HTML, CSS, JS – kein Build). Wird über Vercel ausgeliefert.

Quelle und Notizen liegen im privaten Obsidian-Vault (`_code/ee-design-website/`);
dieses Repo ist der reine Veröffentlichungs-Stand. Änderungen kommen per `sync-ergun-website.ps1` aus dem Vault.
